package net.nk;

import com.google.common.base.Predicate;
import javax.annotation.Nullable;
import net.xn;
import net.nk.n;
import net.yn.wy;

public interface fc extends wy {
   Predicate L = new Predicate() {
      public boolean G(@Nullable net.ne.l var1) {
         return var1 instanceof fc;
      }
   };
   Predicate N = new Predicate() {
      public boolean x(@Nullable net.ne.l var1) {
         net.u.d[] var2 = n.o.y();
         return var1 instanceof fc && !var1.e();
      }

      private static xn a(xn var0) {
         return var0;
      }
   };
}
